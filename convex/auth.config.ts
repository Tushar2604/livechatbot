const authConfig = {
    providers: [
        {
            domain: "https://warm-oarfish-15.clerk.accounts.dev",
            applicationID: "convex",
        },
    ],
};

export default authConfig;
